# Optional desktop window shell (python -m paperless_agent / paperless_agent.desktop).
#
# Source of truth: pyproject.toml [project.optional-dependencies] desktop
# Pins: constraints.txt (./scripts/lock-deps.sh)
--constraint constraints.txt
-e .[desktop]
